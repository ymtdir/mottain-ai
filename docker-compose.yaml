services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mottain-ai
      POSTGRES_USER: mottain-ai
      POSTGRES_PASSWORD: mottain-ai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
