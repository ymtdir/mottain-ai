services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mottain
      POSTGRES_USER: mottain
      POSTGRES_PASSWORD: mottain
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
